# requirements.txt — Project Dependency Manifest

## Overview
This file defines the external Python libraries required to build and run the Dari agentic navigation system. It ensures environment reproducibility by listing the necessary frameworks for the API, data validation, and geospatial reasoning.

## Architecture Role
Layer: Infrastructure/Environment. This file acts as the single source of truth for the project's runtime dependencies, used by package managers (like `pip`) to bootstrap the execution environment.

## Key Concepts (Basics → Advanced)
1. **Web Framework Stack**: The project utilizes `fastapi` and `uvicorn` to provide a high-performance asynchronous REST API for the navigation agent.
2. **Configuration & Validation**: `pydantic` and `pydantic-settings` are used for strict type enforcement and environment variable management, while `python-dotenv` handles the loading of `.env` files.
3. **Geospatial Engine**: The core reasoning capabilities rely on `osmnx` (OpenStreetMap) and `networkx` for graph-based pathfinding and topology analysis, with `geopy` providing distance and coordinate calculations.
4. **Observability & Testing**: `loguru` provides structured logging for agent decision-making traces, and `pytest` is used for validating the reasoning logic.

## Public API / Exports
As a configuration file, this does not export code. However, it defines the following environment capabilities:

| Dependency | Purpose |
| :--- | :--- |
| `fastapi` | Asynchronous web framework for the agent API. |
| `uvicorn` | ASGI server implementation to host the API. |
| `pydantic` | Data validation and settings management. |
| `httpx` | Async HTTP client for external API calls. |
| `loguru` | Advanced logging for system diagnostics. |
| `osmnx` | Retrieval and modeling of street networks. |
| `networkx` | Graph algorithms for navigation reasoning. |
| `geopy` | Geocoding and geodesic distance calculations. |
| `pytest` | Testing framework for system verification. |

## Data Flow
1. **Installation**: The developer or CI/CD pipeline runs `pip install -r requirements.txt`.
2. **Environment Setup**: `python-dotenv` reads the local environment $\rightarrow$ `pydantic-settings` validates the variables.
3. **Runtime**: The application imports these libraries to initialize the `fastapi` app and the `osmnx` graph models.

## Dependencies
- **External Libraries**: All listed items are third-party packages hosted on PyPI.
- **Internal Modules**: None; this file is the root dependency definition.

## Gotchas & Extension Points
- **Version Pinning**: Currently, versions are not pinned (e.g., `fastapi` instead of `fastapi==0.100.0`). This may lead to "breaking change" regressions during new installations. It is recommended to pin versions for production stability.
- **OSMNX Dependencies**: `osmnx` has heavy system-level dependencies (like GDAL or GEOS). Users may need to install these via `apt` or `brew` before running the requirements file.
- **Extension**: To add new agent capabilities (e.g., Vector DBs or LLM SDKs), new entries should be added here and grouped by functionality.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*