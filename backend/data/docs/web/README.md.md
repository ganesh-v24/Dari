# web\README.md — Frontend Project Initialization Guide

## Overview
This file serves as the entry-point documentation for the `web` directory of the Dari project. It provides a high-level overview of the frontend build stack, specifically detailing the use of React and Vite for the user interface.

## Architecture Role
Layer: Infrastructure/Frontend. This file acts as the developer onboarding guide for the presentation layer, defining the build toolchain (Vite) and the UI library (React) used to interface with the agentic navigation system.

## Key Concepts (Basics → Advanced)
1. **Vite Build Tooling**: The project utilizes Vite for extremely fast Hot Module Replacement (HMR) and optimized production bundling.
2. **React Integration**: The system uses the `@vitejs/plugin-react` ecosystem to bridge React components with the Vite build pipeline.
3. **Linting & Quality**: ESLint is integrated to maintain code consistency across the frontend codebase.
4. **Performance Optimization**: The documentation notes the current absence of the React Compiler to prioritize development and build speed.

## Public API / Exports
As this is a Markdown documentation file, it does not export code. However, it references the following technical stack:
- `vite`: The primary build tool and development server.
- `@vitejs/plugin-react`: The official Vite plugin for React support.
- `ESLint`: The static analysis tool for identifying problematic patterns.

## Data Flow
The "data flow" in the context of this file is the developer's onboarding path:
1. **Developer reads README** $\rightarrow$ 2. **Identifies Build Tool (Vite)** $\rightarrow$ 3. **Configures Environment (ESLint/Plugins)** $\rightarrow$ 4. **Executes Development Server**.

## Dependencies
- **React**: The core UI library for building the navigation interface.
- **Vite**: The build tool used for bundling and serving the frontend.
- **ESLint**: Used for maintaining code quality and standards.

## Gotchas & Extension Points
- **TypeScript Migration**: The file explicitly suggests moving toward the TypeScript template for production-grade applications to enable type-aware linting.
- **Compiler Optimization**: A known extension point is the installation of the React Compiler, which is currently disabled to avoid performance degradation during the build process.
- **Plugin Choice**: Developers can choose between the standard React plugin and the SWC-based plugin (`@vitejs/plugin-react-swc`) depending on their preference for compilation speed.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*