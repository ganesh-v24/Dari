# web\eslint.config.js — Static code analysis and linting configuration for the web frontend

## Overview
This file defines the linting rules and environment settings for the Dari web interface. It utilizes the ESLint "Flat Config" format to ensure code quality, enforce React best practices, and maintain a consistent coding style across the frontend codebase.

## Architecture Role
- **Layer**: Tooling / Infrastructure.
- **Pattern**: Configuration-as-Code.
- **Dependency direction**: This file is consumed by the ESLint CLI and IDE plugins; it imports rules from the ESLint ecosystem and applies them to the `web` directory source files.

## Key Concepts (Basics → Advanced)
1. **Flat Configuration**: The file uses `defineConfig`, representing the modern ESLint configuration system which replaces the legacy `.eslintrc` format for better performance and composability.
2. **Global Ignores**: The `globalIgnores(['dist'])` call ensures that the build output directory is completely ignored by the linter to prevent noise and performance degradation.
3. **Plugin Integration**: 
    - `eslint-plugin-react-hooks`: Enforces the "Rules of Hooks" (e.g., ensuring hooks are called at the top level).
    - `eslint-plugin-react-refresh`: Optimizes the development experience by ensuring components are compatible with Vite's Fast Refresh.
4. **Environment Scoping**: The `languageOptions` explicitly set `globals.browser`, informing the linter that browser-specific globals (like `window` or `document`) are valid and should not be flagged as undefined.

## Public API / Exports
| Export | Type | Description |
| :--- | :--- | :--- |
| `default` | Array/Config | The exported ESLint configuration object used by the ESLint engine to lint `.js` and `.jsx` files. |

## Data Flow
1. **Initialization**: ESLint reads `eslint.config.js` upon execution.
2. **Filtering**: The engine checks `globalIgnores` to skip the `dist` folder.
3. **Targeting**: The `files` pattern `**/*.{js,jsx}` identifies which files in the project should be processed.
4. **Rule Application**: The engine merges the `recommended` configs from JavaScript, React Hooks, and React Refresh.
5. **Validation**: The source code is parsed using the JSX-enabled parser and validated against the combined rule set.

## Dependencies
- `@eslint/js`: Provides the standard recommended JavaScript rules.
- `globals`: Provides predefined lists of global variables for different environments (Browser in this case).
- `eslint-plugin-react-hooks`: Ensures correct usage of React Hooks.
- `eslint-plugin-react-refresh`: Ensures components are compatible with Hot Module Replacement (HMR).
- `eslint/config`: Provides utility functions like `defineConfig` and `globalIgnores` for type-safe configuration.

## Gotchas & Extension Points
- **File Extensions**: Currently, this config only targets `.js` and `.jsx`. If the project migrates to TypeScript (`.ts`, `.tsx`), the `files` array and the `parserOptions` will need to be updated to include a TypeScript parser (e.g., `@typescript-eslint/parser`).
- **Custom Rules**: To add project-specific restrictions (e.g., forbidding `console.log`), a `rules` object should be added inside the configuration block.
- **Strictness**: The current setup uses `recommended` presets; for stricter architectural enforcement, "plugin-plugin" or "airbnb" style configurations could be integrated.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*