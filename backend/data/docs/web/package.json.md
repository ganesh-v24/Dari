# web\package.json — Project Manifest and Dependency Configuration

## Overview
This file serves as the manifest for the `web` directory, defining the environment, dependencies, and build scripts for the Dari project's frontend. It configures a modern React application powered by Vite, specifically tailored for geospatial visualization.

## Architecture Role
Layer: Infrastructure/Configuration. This file defines the runtime environment and toolchain for the User Interface layer. It ensures that the frontend has the necessary libraries to render maps and manage the UI state, acting as the entry point for dependency resolution.

## Key Concepts (Basics → Advanced)
1. **Module System**: The `"type": "module"` field indicates that the project uses ES Modules (ESM), allowing for the use of `import` and `export` statements natively.
2. **Build Tooling**: The project utilizes **Vite**, a high-performance build tool that provides a fast development server and optimized production bundling.
3. **Geospatial Stack**: The core functionality relies on **Leaflet** (the underlying mapping engine) and **react-leaflet** (the React wrapper), enabling the agentic navigation reasoning system to be visualized on an interactive map.
4. **Type Safety and Linting**: The inclusion of `@types` and `eslint` ensures code quality and provides static type checking for React components during development.

## Public API / Exports
As a JSON configuration file, this does not export functions, but it defines the following operational interfaces:

| Script | Command | Description |
| :--- | :--- | :--- |
| `dev` | `vite` | Starts the local development server with Hot Module Replacement (HMR). |
| `build` | `vite build` | Compiles the application into optimized static assets for production. |
| `lint` | `eslint .` | Analyzes the codebase for programmatic and stylistic errors. |
| `preview` | `vite preview` | Locally boots the production build to verify deployment readiness. |

## Data Flow
1. **Installation**: `npm install` or `yarn` reads this file to fetch dependencies from the registry.
2. **Development**: The `dev` script triggers Vite, which reads this file to resolve module paths and apply the `@vitejs/plugin-react` transformation.
3. **Compilation**: The `build` script processes the source code, bundling the `react` and `leaflet` dependencies into a minified browser-ready package.

## Dependencies
### Production Dependencies
- `leaflet`: The core JavaScript library for interactive maps.
- `react` & `react-dom`: The UI library and DOM renderer for building the interface.
- `react-leaflet`: Provides React components for Leaflet maps, bridging the imperative Leaflet API with the declarative React model.

### Development Dependencies
- `vite` & `@vitejs/plugin-react`: The build engine and its React integration plugin.
- `eslint` & plugins: Tools for maintaining code standards and catching bugs.
- `@types/react` & `@types/react-dom`: TypeScript definitions for React to provide IDE intellisense.

## Gotchas & Extension Points
- **Version Sensitivity**: The project uses React 19. Ensure any new UI libraries added are compatible with the React 19 RC/Stable versions to avoid peer dependency conflicts.
- **Leaflet CSS**: Note that `leaflet` requires a CSS file to be imported in the source code (usually `leaflet.css`); this `package.json` provides the library, but the styles must be linked manually in the application.
- **Extension**: To add state management (e.g., Zustand or Redux) or API clients (e.g., Axios), they should be added to the `dependencies` section.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*