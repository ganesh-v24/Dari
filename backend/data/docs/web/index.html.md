# web\index.html — Main Entry Point for the Web Frontend

## Overview
This file serves as the root HTML document for the Dari web application. It provides the basic DOM structure, loads essential external stylesheets for mapping and typography, and bootstraps the React application by linking to the main JavaScript entry point.

## Architecture Role
- **Layer**: Presentation / View (Entry Point).
- **Pattern**: Single Page Application (SPA) Shell.
- **Dependency direction**: This is the top-level file loaded by the browser; it triggers the execution of `/src/main.jsx`.

## Key Concepts (Basics → Advanced)
1. **SPA Mounting Point**: The `<div id="root"></div>` acts as the unique anchor where the React virtual DOM is injected and rendered.
2. **External Asset Integration**: It integrates third-party CSS (Leaflet) directly in the head to ensure map styles are available before the JavaScript components attempt to render the map.
3. **Module Loading**: The use of `type="module"` in the script tag allows the browser to handle ES modules, enabling the use of modern import/export syntax in the application source.

## Public API / Exports
As an HTML file, it does not export code, but it defines the following global hooks:
- `div#root`: The DOM element used by the React application for mounting.
- `title`: Sets the browser tab label to "Dari - Map & Navigation".

## Data Flow
1. **Request**: Browser requests `index.html` from the web server.
2. **Resource Loading**: Browser fetches and parses CSS (Leaflet and Google Fonts).
3. **Script Execution**: Browser encounters `<script src="/src/main.jsx">` and initiates the JavaScript execution pipeline.
4. **Mounting**: The React application (via `main.jsx`) targets the `root` div to render the agentic navigation UI.

## Dependencies
- **Leaflet CSS**: Required for the rendering and styling of the interactive map components.
- **Google Fonts (Inter)**: Provides the standardized typography used across the Dari user interface.
- `/src/main.jsx`: The internal JavaScript entry point that contains the application logic.

## Gotchas & Extension Points
- **CSS Specificity**: Because Leaflet CSS is loaded here, any custom map overrides in the React components must be written with sufficient specificity to override these global styles.
- **Extension**: To add global meta tags for SEO or PWA (Progressive Web App) capabilities, they should be added within the `<head>` section of this file.
- **Performance**: If the application grows, consider moving the Google Fonts import to a local asset or using a preconnect hint to reduce render-blocking time.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*