# backend\agents\base.py — Abstract base class for all AI agents

## Overview
This file defines the `BaseAgent` abstract base class, which establishes the foundational contract for all agentic entities within the Dari system. It ensures that every agent, regardless of its specific reasoning logic, implements a consistent execution interface.

## Architecture Role
- **Layer**: Core / Base Layer.
- **Pattern**: Template Method / Interface.
- **Dependency direction**: This is a leaf dependency; it is imported by all concrete agent implementations (e.g., NavigationAgent, ReasoningAgent) but depends on no other internal project modules.

## Key Concepts (Basics → Advanced)
1. **Abstract Base Class (ABC)**: By inheriting from `ABC`, the system prevents `BaseAgent` from being instantiated directly, forcing developers to create specialized subclasses.
2. **Interface Standardization**: The use of the `@abstractmethod` decorator on `execute` guarantees that any agent integrated into the navigation reasoning system will have a predictable entry point.
3. **Asynchronous Execution**: The `execute` method is defined as `async`, acknowledging that AI agents typically perform I/O-bound operations, such as calling Large Language Models (LLMs) or querying external map APIs.

## Public API / Exports
| Export | Type | Description |
| :--- | :--- | :--- |
| `BaseAgent` | Class | The abstract base class that defines the required structure for all agents. |
| `BaseAgent.execute` | Method | An abstract asynchronous method that processes `input_data` and returns a dictionary of results. |

## Data Flow
1. **Initialization**: An agent is instantiated with a `name` string.
2. **Input**: The `execute` method receives `input_data` (of type `Any`), which could be a user query, a coordinate, or a state object.
3. **Transformation**: The concrete subclass implements the specific reasoning or action logic.
4. **Output**: The method returns a `Dict[str, Any]`, providing a flexible schema for agents to return multiple values (e.g., `{"status": "success", "path": [...]}`).

## Dependencies
- `abc`: Used to define the Abstract Base Class and abstract methods.
- `typing`: Used for type hinting (`Any`, `Dict`) to ensure maintainable and readable code.

## Gotchas & Extension Points
- **Generic Input/Output**: Because `input_data` is typed as `Any`, developers must ensure that the data passed to a specific agent matches what that agent's `execute` implementation expects.
- **Extension Point**: To add shared functionality across all agents (e.g., logging, telemetry, or memory management), methods should be added to `BaseAgent` as concrete methods rather than abstract ones.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*