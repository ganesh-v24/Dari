# backend\app\main.py — Application Entry Point and Lifecycle Manager

## Overview
This file serves as the primary entry point for the Dari backend. It initializes the FastAPI application, configures global middleware, sets up system-wide logging, and manages the orchestration of the agentic ecosystem during the server's startup and shutdown phases.

## Architecture Role
- **Layer**: Application/Infrastructure.
- **Pattern**: Factory Pattern (`create_app`) and Lifespan Pattern.
- **Dependency direction**: Receives calls from the ASGI server (e.g., Uvicorn); calls `core.config`, `core.logging`, `api.routes`, and the `agents` package.

## Key Concepts (Basics → Advanced)

1. **FastAPI Application Factory**: The `create_app()` function encapsulates the instantiation of the `FastAPI` object, allowing for better testability and configuration management.
2. **Lifespan Management**: Using the `@asynccontextmanager` decorator on the `lifespan` function, the system handles logic that must occur before the server starts accepting requests (startup) and after it stops (shutdown).
3. **Agent Registration**: The `_register_agents()` function acts as a bootstrapper. It instantiates the various specialized agents (e.g., `DariAgent`, `RouteAgent`), which utilize a self-registration pattern to populate the global `AgentRegistry`.
4. **Non-blocking Background Tasks**: The use of `asyncio.create_task(_run_document_agent_on_startup())` demonstrates an asynchronous "fire-and-forget" pattern, ensuring that heavy initialization tasks (like updating documentation) do not delay the API's availability.

## Public API / Exports

| Export | Type | Description |
| :--- | :--- | :--- |
| `app` | `FastAPI` | The singleton instance of the FastAPI application used by the ASGI server. |
| `create_app` | `Function` | Factory function that configures and returns a new `FastAPI` instance. |
| `lifespan` | `AsyncContextManager` | Manages the startup and shutdown sequence of the backend. |

## Data Flow

1. **Initialization**: The ASGI server calls `app`, triggering `create_app()`.
2. **Startup Sequence**:
    - `setup_logging()` is called to initialize `loguru`.
    - `_register_agents()` is executed, instantiating agent classes and adding them to the `registry`.
    - An asynchronous task is spawned to run `_run_document_agent_on_startup()`.
3. **Request Handling**: Incoming HTTP requests are routed via the `router` (imported from `api.routes`) or the `/health` endpoint.
4. **Shutdown**: When the server receives a termination signal, the `lifespan` generator resumes after the `yield` statement to log the shutdown event.

## Dependencies

- `fastapi`: The core web framework for building the API.
- `asyncio`: Used for managing the background startup task and asynchronous delays.
- `core.config` / `core.logging`: Provides system settings and standardized logging via `loguru`.
- `api.routes`: Provides the API endpoint definitions.
- `agents.*`: The suite of AI agents (Dari, Document, Route, Locality, UserMemory, Planner) and the `registry` used to manage them.

## Gotchas & Extension Points

- **Circular Imports**: Note that agent imports are performed *inside* `_register_agents()` and `_run_document_agent_on_startup()`. This is a deliberate design choice to avoid circular dependency issues between the main app and the agent registry.
- **Startup Delay**: There is a hardcoded `asyncio.sleep(3)` in the document agent startup task. If the system scales or requires faster boot times, this may need to be replaced with a more robust readiness check.
- **CORS Policy**: The current `CORSMiddleware` is configured with `allow_origins=["*"]`, which is suitable for development but should be restricted to specific domains in a production environment.
- **Extension**: To add a new agent to the system, simply import it and instantiate it within the `_register_agents()` function.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*