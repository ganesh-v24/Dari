# backend\core\config.py — Centralized Application Configuration

## Overview
This file defines the global configuration schema for the Dari system using Pydantic's `BaseSettings`. It ensures that environment variables are validated, typed, and accessible as a singleton object throughout the backend.

## Architecture Role
- **Layer**: Infrastructure / Core
- **Pattern**: Singleton / Configuration Object
- **Dependency direction**: This is a leaf node in the dependency graph; it is imported by almost every other module in the system but does not import from other project modules.

## Key Concepts (Basics → Advanced)
1. **Environment Variable Mapping**: The file uses `pydantic_settings` to automatically map system environment variables to Python class attributes.
2. **Type Safety**: By declaring types (e.g., `str`, `str | None`), the system ensures that configuration errors are caught at startup rather than during runtime.
3. **Defaulting**: It provides sensible defaults for development (e.g., `APP_ENV = "dev"`) while allowing overrides via a `.env` file.
4. **The `.env` Integration**: The inner `Config` class instructs Pydantic to look for a file named `.env` in the root directory to populate the settings.

## Public API / Exports
| Export | Type | Description |
| :--- | :--- | :--- |
| `Settings` | Class | The Pydantic model defining all available configuration keys. |
| `settings` | Instance | A singleton instance of `Settings` used across the application. |

## Data Flow
1. **Initialization**: When `settings = Settings()` is called, Pydantic initializes.
2. **Loading**: The system checks for a `.env` file and reads key-value pairs.
3. **Override**: System-level environment variables take precedence over values found in the `.env` file.
4. **Validation**: Pydantic validates that the values match the declared types.
5. **Consumption**: Other modules import the `settings` object to access values (e.g., `settings.MAPS_API_KEY`).

## Dependencies
- `pydantic_settings`: Used for the `BaseSettings` class to handle environment variable parsing and validation.

## Gotchas & Extension Points
- **Missing Keys**: `MAPS_API_KEY` is marked as `str | None`, meaning the application will start even if the key is missing. If this key becomes mandatory for a specific feature, the type should be changed to `str` to trigger a validation error on startup.
- **Extension**: To add new configuration parameters (e.g., Database URLs or LLM Model names), simply add a new typed attribute to the `Settings` class.
- **Environment Switching**: The `APP_ENV` variable can be used in other modules to toggle between `dev`, `staging`, and `prod` logic.

---
*Auto-generated by DocumentAgent on 2026-05-10 using gemma4:31b:cloud.*